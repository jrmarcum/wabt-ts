{
  "name": "wabt-ts",
  "description": "Native TypeScript port of WebAssembly/wabt — see deno.json for primary config",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
